
language: python
python:
  - "2.6"
  - "2.7"

before_install:
  - sudo apt-get install -qq spatialite-bin

install:
  - "pip install -r requirements.txt --use-mirrors"

script: "python manage.py test"